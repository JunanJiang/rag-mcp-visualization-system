# Plot3D 知识库补充卡片（解释性增强）

这组卡片用于解决“报告只有数据拼接、缺少解释性文字”的问题：
- 不提供写作技巧；只提供**变量/后处理/结果解读**相关的背景知识与可读解释口径。
- 适合在 RAG 中按 metadata 过滤（report_modules/slot_intents）检索。

建议用法：
1) 解析数据包生成 run_facts.json（变量范围、是否存在 VelocityVector、图片路径、ops 参数等）。
2) 每个槽位填充时：先检索这些卡片 + 原背景卡片，再把 facts 一起喂给 LLM 生成 120~200 字解释。
