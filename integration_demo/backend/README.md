# Integration Demo Backend

Run:

```bash
python app.py
```

Default port: `5100`

Environment variables:

- `SIMUREPORT_BACKEND_URL` default `http://127.0.0.1:5000`
- `SIMUREPORT_FRONTEND_URL` default `http://localhost:3001`
- `INTEGRATION_DEMO_SECRET` optional Flask session secret
